<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://www.springframework.org/schema/security"
         xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
    http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<!-- spring-security-example -->
	<!--
    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/app/admin/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/app/authentication/login" access="permitAll()"/>
        <intercept-url pattern="/app/authentication/loginfailed" access="permitAll()"/>
        <intercept-url pattern="/app/**" access="isAuthenticated()"/>
        <form-login login-page="/app/authentication/login" default-target-url="/app/main/index"
                    authentication-failure-url="/app/authentication/loginfailed"/>
        <logout logout-url="/app/authentication/logout" logout-success-url="/app/authentication/login"/>
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="userDetailService">
            <password-encoder hash="md5"/>
        </authentication-provider>
    </authentication-manager>	
	-->
	
</b:beans>